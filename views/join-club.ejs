<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>

    <style>
      /* Estilos rápidos para que los errores se vean claros */
      .errors {
        list-style: none;
        padding: 10px;
        margin: 10px 0;
        border: 1px solid #a83232;
        background: #f7d4d4;
        color: #a83232;
        border-radius: 5px;
      }
    </style>
  </head>
  <body>
    <h1><%= title %></h1>

    <p>Hello, <strong><%= currentUser.firstName %></strong>!</p>
    <p>Enter the secret passcode to become a full member.</p>

    <% if (typeof errors !== 'undefined' && errors.length > 0) { %>
    <ul class="errors">
      <% errors.forEach(error => { %>
      <li><%= error.msg %></li>
      <% }) %>
    </ul>
    <% } %>

    <form action="/join-club" method="POST">
      <div>
        <label for="passcode">Secret Passcode:</label>
        <input type="password" id="passcode" name="passcode" required />
      </div>

      <button type="submit">Join</button>
    </form>
  </body>
</html>
